# Изменения в версии YaLedger 0.1.2.0

-   \#99: в конфиге можно задавать десятичный разделитель (обычно точка
    или запятая) и разделитель разрядов (обычно пробел). Эти параметры
    используются при парсинге чисел.
-   \#110: запись «root /путь/к группе» задаёт группу счетов,
    относительно которой ищутся все счета.
-   \#111: пользовательские сообщения в случае несоответствия сумм в
    транзакции reconciliate.
-   \#112: запись «attributes {..}» задаёт атрибуты, которые добавляются
    по умолчанию к последующим записям.
-   \#113: расширен синтаксис записи, добавлено новое поле — status.

